import 'package:flutter/material.dart';

class CalculatePriceAndTime extends StatelessWidget {
  const CalculatePriceAndTime({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Text('data');
  }
}
