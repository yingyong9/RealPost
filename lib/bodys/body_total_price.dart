import 'package:flutter/material.dart';

class BodyTotalPrice extends StatelessWidget {
  const BodyTotalPrice({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('total price');
  }
}